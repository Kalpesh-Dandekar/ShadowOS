// Demo identity records are intentionally deferred until Phase 2B introduces
// password hashing. Never seed plaintext or placeholder values into passwordHash.
console.log("ShadowOS identity seed deferred until Phase 2B password hashing is available.");
