import {Router}from"express";import{getExecution,getRollback,rollback}from"../controllers/executionController.js";import{authenticate}from"../middleware/authenticate.js";import{validate,validateParams}from"../middleware/validate.js";import{emptyExecutionBodySchema,executionParamsSchema}from"../schemas/executionSchemas.js";
export const executionRouter=Router();executionRouter.use(authenticate);executionRouter.get("/:executionId",validateParams(executionParamsSchema),getExecution);executionRouter.post("/:executionId/rollback",validateParams(executionParamsSchema),validate(emptyExecutionBodySchema),rollback);executionRouter.get("/:executionId/rollback",validateParams(executionParamsSchema),getRollback);
